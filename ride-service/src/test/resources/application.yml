spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rides
    password: FLRPVcp41NC7mwD2TtX5
    username: root
  jpa:
    hibernate:
      ddl-auto: create
  liquibase:
    enabled: false
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: ride-service-group
    ride-producer-topic:
      name: test-rides-topic
    status-producer-topic:
      name: test-driver-status-topic
    ride-consumer-topic:
      name: test-ride-with-driver-topic
eureka:
  client:
    enabled: false
feign:
  client:
    driver:
      name: driver-service
      url: /api/v1/drivers
    passenger:
      name: passenger-service
      url: /api/v1/passengers
    payment:
      name: payment-service
      url: /api/v1/payments
    promocode:
      name: promocode-service
      url: /api/v1/promocodes